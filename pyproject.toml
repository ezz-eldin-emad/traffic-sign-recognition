[project]
name = "venv"
version = "0.1.0"
requires-python = ">=3.10,<3.13"
dependencies = [
    "numpy",
    "pandas",
    "scikit-learn",
    "matplotlib",
    "seaborn",
    "streamlit",
    "kagglehub",
]

[project.optional-dependencies]

cpu = [
    "tensorflow-cpu",
]

cuda = [
    "tensorflow[and-cuda]",
]
